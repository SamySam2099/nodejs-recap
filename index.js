import { posts } from './postController.js'

// We don't have window or document (dom) as we are not on browser
// console.log(window)
// const generateRandomNum = require('./utils')

console.log(posts)
